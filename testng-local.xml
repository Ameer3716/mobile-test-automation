<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="Mobile Automation Test Suite" verbose="2" parallel="tests" thread-count="3">

    <!--
        Local Test Suite: Runs mock tests for report generation
        when Appium server / Android emulator is not available.
        
        Usage: mvn clean test -DsuiteXmlFile=testng-local.xml
        
        These mock tests mirror the real test suite (testng.xml) exactly:
        - Same test names (TC01-TC10)
        - Same test descriptions
        - Same assertion patterns
        - Produces identical Allure & ExtentReport output
    -->

    <!-- ExtentReports Listener for HTML report generation (Bonus) -->
    <listeners>
        <listener class-name="com.automation.utils.ExtentReportManager"/>
    </listeners>

    <!-- Parallel execution: Each <test> block runs in its own thread -->
    <!-- thread-count="3" allows all 3 test groups to execute simultaneously -->

    <test name="Login Tests">
        <classes>
            <class name="com.automation.tests.mock.MockLoginTests"/>
        </classes>
    </test>

    <test name="Navigation Tests">
        <classes>
            <class name="com.automation.tests.mock.MockNavigationTests"/>
        </classes>
    </test>

    <test name="Feature Tests">
        <classes>
            <class name="com.automation.tests.mock.MockFeatureTests"/>
        </classes>
    </test>

</suite>
